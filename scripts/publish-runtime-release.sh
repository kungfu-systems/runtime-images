#!/bin/bash
# SPDX-License-Identifier: Apache-2.0
set -euo pipefail
echo "The v3 shell publisher is retired. Buildchain v4 sealed OCI publication owns release writes." >&2
echo "Use the protected alpha PR and the exact-tag qualification workflow." >&2
exit 2
