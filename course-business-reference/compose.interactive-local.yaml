name: kungfu-course-business-reference

# Compatibility overlay. Local model management, llama.cpp, and the persistent
# model volume are now built into the unified application image and base
# Compose file. A model is still downloaded only after an authenticated user
# explicitly clicks Download.
services:
  app:
    environment:
      COURSE_LOCAL_MODEL_MANAGEMENT: "true"
