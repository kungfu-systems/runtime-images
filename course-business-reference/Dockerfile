# syntax=docker/dockerfile:1.7
# SPDX-License-Identifier: Apache-2.0

# Compatibility wrapper only. The repository publishes and builds the first-party
# application exclusively through ../Dockerfile.
ARG KUNGFU_HUB_IMAGE
FROM ${KUNGFU_HUB_IMAGE}
