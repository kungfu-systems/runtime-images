name: kungfu-course-business-reference

# Compatibility overlay retained for older commands. It no longer downloads a
# model at startup: the unified product has one image and requires explicit,
# authenticated click-to-install model delivery.
services:
  app:
    environment:
      COURSE_LOCAL_MODEL_MANAGEMENT: "true"
